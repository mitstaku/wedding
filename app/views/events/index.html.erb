<%= render "shared/header" %>

    <% @events.each do |event| %>
      <div class='event-info'>
        <h3 class='event-name'>
          <%= event.name %>
        </h3>
        <div class='event-date'>
          <%= event.event_date %>
        </div>
      </div>
    <% end %>
